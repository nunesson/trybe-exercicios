let car = {
  type: 'Peugeot',
  model: '3008',
  color: 'Cinza',
};

console.log(`O meu carro vai ser um ${car.type} ${car.model} ${car.color}`); //template string

console.log('O meu carro vai ser um ' + car.type + ' ' + car.model + ' ' + car.color); //template noob